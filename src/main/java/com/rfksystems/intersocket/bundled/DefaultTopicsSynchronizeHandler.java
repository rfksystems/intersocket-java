package com.rfksystems.intersocket.bundled;

import com.rfksystems.intersocket.TopicsSynchronizeHandler;

import java.util.Set;

public class DefaultTopicsSynchronizeHandler<T> implements TopicsSynchronizeHandler<T> {
    @Override
    public void handle(final Set<String> topics, final T scope) {

    }
}
